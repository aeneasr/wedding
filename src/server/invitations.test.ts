// createInvitationFromRegistration and saveGuestRsvp contactPhone handling
// are covered by E2E tests in tests/e2e/register.spec.ts — server DB test
// infra (real Postgres in Vitest) is not yet set up.
import { it } from "vitest";

it.todo(
  "createInvitationFromRegistration: covered by e2e until server DB test infra lands",
);
